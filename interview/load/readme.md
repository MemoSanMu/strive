# 面试记录

## 留涛

```js
  1.自我介绍，我讲了工作履历，项目经历 2.面试官提问
  (3 分钟)
  (30 分钟左右)
  (1) 技术选型为什么用了 taro，遇到过哪些问题，怎么解决?
  我根据过往项目经验回答(2) 小程序优化做了哪些?我回答了 setData 优化，讲了 diff 的策略，还有包大小优化等等(3) 白屏监控的方案设计
  我讲了整体方案设计，以及告警配置等等，他问了一些细节的实现
  (4) 商品列表加载的优化 (我简历里没写)
  我先讲了我们是按列计算高度来实现的瀑布流，并在此基础上来做了虚拟列表的实现，他问了一些细节的实现，我就讲了讲
  (5) 你现在的职位，纯开发，还是有带团队
  回答，目前是小组长，组内三个人
  设计一个 load 函数，满足以下要求 3
  (10 分钟)
  load(1).then(console.log)
  load(2).then(console.log)
  load(3).then(console.log)
  load(4).then(console.log)输出顺序按照调用顺序返回， 每次异步的时间随机，大约 10 分钟，我没写出来
  讲一下事件循环(5 分钟)4 我就回答了一下事件循环，宏任务和微任务，执行机制等他补充问了 setTimeout 这种宏任务是在本次循环执行，还是下次循环执行，我回答下一次然后给了一个事件循环的题，让我回答输出顺序，我也是有个函数执行看错顺序了，说反了一个，其余的正常 5
  虚拟 dom 一定比传统 dom 性能好吗?(3 分钟)我回答不是，讲了虚拟 dom 的作用，框架选择用虚拟 d 是在做权衡，本质是因为 s 的执行要远比操作 dom 少消耗性能，特别简单的交互，直接操作 dom，反而更高效
  (3 分钟)
  讲讲 promise 常用的方法 race 之类的，都有什么区别?6
  我回答了 race，all，allSettled 以及各自的作用设计一个 超时函数(5 分钟)支持传入 promise 和 time，如果超时了，就返回函数超时，如果没有,就返回 promise 的执行结果 我写了一半，说时间要到了，问我有没有要问的，最后提醒我回来再多看看 promise 备注: 代码都是在牛客网写的，不让用自己的编辑器
```
